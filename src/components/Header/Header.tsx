import Logo from '@/components/Logo';
import Profile from '@/components/Profile';

import './header.css';

const Header = () => (
  <header>
    <Logo />
    <Profile />
  </header>
);

export default Header;
